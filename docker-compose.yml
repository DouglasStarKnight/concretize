services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: concretize
    ports:
      - "8090:80"
    volumes:
      - ./:/var/www/html:cached
    depends_on:
      - mysql
    networks:
      - laravelnet

  mysql:
    image: mysql:8.0
    container_name: mysqlconcretize
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravel
      MYSQL_ROOT_HOST: "%"  # Permitir conexões externas
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - laravelnet

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadminconcretize
    ports:
      - "8091:80"
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
    depends_on:
      - mysql
    networks:
      - laravelnet

volumes:
  mysql_data:

networks:
  laravelnet:
    driver: bridge